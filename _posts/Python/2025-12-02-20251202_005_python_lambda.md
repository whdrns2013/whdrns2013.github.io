---
title: "[Python] 람다 함수 lambda" # 제목 (필수)
excerpt: "함수를 따로 정의하지 않고 즉시 사용할 수 있는 익명 함수" # 서브 타이틀이자 meta description (필수)
date: 2025-12-02 06:10:00 +0900      # 작성일 (필수)
lastmod: 2025-12-02 06:10:00 +0900   # 최종 수정일 (필수)
last_modified_at: 2025-12-02 06:10:00 +0900   # 최종 수정일 (필수)
categories: Python          # 다수 카테고리에 포함 가능 (필수)
tags: typography 파이썬 람다 익명함수 익명 함수 lambda                  # 태그 복수개 가능 (필수)
classes: wide        # wide : 넓은 레이아웃 / 빈칸 : 기본 //// wide 시에는 sticky toc 불가
toc: true        # 목차 표시 여부
toc_label:       # toc 제목
toc_sticky: true # 이동하는 목차 표시 여부 (toc:true 필요) // wide 시에는 sticky toc 불가
header: 
  image:         # 헤더 이미지 (asset내 혹은 url)
  teaser:        # 티저 이미지??
  overlay_image: /assets/images/banners/banner.gif            # 헤더 이미지 (제목과 겹치게)
  # overlay_color: '#333'            # 헤더 배경색 (제목과 겹치게) #333 : 짙은 회색 (필수)
  video:
    id:                      # 영상 ID (URL 뒷부분)
    provider:                # youtube, vimeo 등
sitemap :                    # 구글 크롤링
  changefreq : daily         # 구글 크롤링
  priority : 1.0             # 구글 크롤링
author: # 주인 외 작성자 표기 필요시
permalink: 
sidebar:
  nav: docs_python
---
<!--postNo: 20251202_005-->

## 람다 함수  

### 정의  

- lambda   
- 이름이 없는 익명 함수 (anonymous function)  
- 함수를 따로 정의하지 않고 **사용시에 정의해 즉시 사용할 수 있는 익명 함수**  

### 문법적 특징  

- 단 하나의 표현식만 허용한다. 즉, 일반 함수처럼 내부에 여러 줄(표현식)을 사용할 수 없다.  
- 표현식의 결과는 자동으로 함수의 반환값이 된다. (return 필요 없음)  

### 장점  

- 간결함 : 한 줄의 표현식으로 작성할 수 있음  
- 불필요한 이름 방지 : 일회성 사용  
- 고차함수와 결합해 사용할 때 유용(??)  
- 함수형 프로그래밍 스타일 지원  

### 기본 문법  

```python
lambda 매개변수(들):수행할표현식
```

### 활용 예시  

- 일반 함수 사용

```python
def add(x):
    return x + 2
print(add(3)
>> 5
```

- lambda 사용  

```python
print((lambda x:x+2)(3))
>> 5
```

- 혹은, 변수에 람다 할당 가능  

```python
add_lambda = lambda x: x + 2
print(add_lambda(3))
>> 5
```

### 확장 예제  

```python
print((lambda x,y: x + y)(2, 3))
>> 5
```


### 활용 예시  

```python
employees = {"이지혜": 3000000, "구민준": 5000000, "방서연": 4100000}
updated_salaries = {name: (lambda salary:int(salary*1.1))(salary) for name, salary in employees.items()}
print(updated_salaries)
>> {'이지혜': 3300000, '구민준': 5500000, '방서연': 4510000}
```

## Reference  

방송통신대 - 오픈소스기반 데이터분석 (정재화)