---
title: "[Python] reduce 함수" # 제목 (필수)
excerpt: "iterate 객체의 모든 요소를 누적 계산하는 함수" # 서브 타이틀이자 meta description (필수)
date: 2025-12-02 06:40:00 +0900      # 작성일 (필수)
lastmod: 2025-12-02 06:40:00 +0900   # 최종 수정일 (필수)
last_modified_at: 2025-12-02 06:40:00 +0900   # 최종 수정일 (필수)
categories: Python          # 다수 카테고리에 포함 가능 (필수)
tags: typography 파이썬 reduce 누적 계산                   # 태그 복수개 가능 (필수)
classes: wide        # wide : 넓은 레이아웃 / 빈칸 : 기본 //// wide 시에는 sticky toc 불가
toc: true        # 목차 표시 여부
toc_label:       # toc 제목
toc_sticky: true # 이동하는 목차 표시 여부 (toc:true 필요) // wide 시에는 sticky toc 불가
header: 
  image:         # 헤더 이미지 (asset내 혹은 url)
  teaser:        # 티저 이미지??
  overlay_image: /assets/images/banners/banner.gif            # 헤더 이미지 (제목과 겹치게)
  # overlay_color: '#333'            # 헤더 배경색 (제목과 겹치게) #333 : 짙은 회색 (필수)
  video:
    id:                      # 영상 ID (URL 뒷부분)
    provider:                # youtube, vimeo 등
sitemap :                    # 구글 크롤링
  changefreq : daily         # 구글 크롤링
  priority : 1.0             # 구글 크롤링
author: # 주인 외 작성자 표기 필요시
permalink: 
sidebar:
  nav: docs_python
---
<!--postNo: 20251202_008-->

## reduce 함수  

### 정의  

- 반복 가능한 객체의 모든 요소를 **특정 함수로 누적 계산해 하나의 값으로 축소하는 함수**  
- 반복문을 사용하지 않고 데이터를 하나의 값으로 집계  
- 기본 내장함수는 아니고, functools 모듈에서 제공함  

### 기본 문법  

```python
from functools import reduce
reduce(함수, iterate객체)
```

- 작동 방식  

(1) 첫 번째와 두 번째 요소를 함수에 적용해 중간 결과를 생성한다.  
(2) 그 중간 결과와 다음 요소를 다시 함수에 적용해 2차 중간 결과를 생성한다.  
(3) 1-2 과정을 반복해 최종적으로 하나의 값을 변환한다.  

### 활용 예시  

```python
from functools import reduce
numbers = [1, 2, 3, 4, 5]
print(reduce(lambda x,y: x + y, numbers))
```

만약 반복문으로 작성했다면  

```python
numbers = [1, 2, 3, 4, 5]
sum = 0

for number in numbers:
    sum += number

print(number)
```

## Reference  

방송통신대 - 오픈소스기반 데이터분석 (정재화)